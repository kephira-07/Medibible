# En local, laisse vide — le proxy Vite (vite.config.js) route déjà /api et
# /socket.io vers http://localhost:5000. Renseigne cette valeur uniquement en
# production, sur Vercel : URL du serveur déployé sur Render, SANS slash final.
VITE_API_URL=https://medibible-api.onrender.com
